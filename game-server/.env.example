# ── LOCAL (docker compose) ────────────────────────────────
DATABASE_URL="postgresql://rad:rad_dev_only@localhost:5432/rad?schema=public"
DIRECT_URL="postgresql://rad:rad_dev_only@localhost:5432/rad?schema=public"

# ── SUPABASE (produção) ───────────────────────────────────
# ⚠️ Crie o projeto na região South America (São Paulo). NÃO PODE MUDAR DEPOIS.
# DATABASE_URL = pooler 6543 → runtime
# DIRECT_URL   = direta 5432 → migrations
#
# DATABASE_URL="postgresql://postgres.REF:SENHA@aws-0-sa-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=10"
# DIRECT_URL="postgresql://postgres:SENHA@db.REF.supabase.co:5432/postgres"

REDIS_URL="redis://localhost:6379"
JWT_SECRET="troque-isto-antes-de-qualquer-deploy"
JWT_ACCESS_TTL="15m"
JWT_REFRESH_TTL="30d"
PORT=3000
